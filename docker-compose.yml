services:
  app:
    build: .
    container_name: sar_backend_app
    volumes:
      - 'C:\Users\User\.config\earthengine:/root/.config/earthengine'

  ngrok:
    image: ngrok/ngrok:latest
    container_name: sar_ngrok_tunnel
    ports:
      - "4040:4040"
    depends_on:
      - app
    env_file:
      - .env
    # [Ultimate Final Fix] Use the ABSOLUTELY simplest command.
    # ALL header modifications are removed. CORS is now handled 100% by FastAPI.
    command:
      - "http"
      - "app:8000"
      - "--log=stdout"
      - "--basic-auth=nasa:spaceapps2025"
      - "--domain=juliette-unattempted-tammara.ngrok-free.dev"

services:
  app:
    build: .
    container_name: sar_backend_app
    volumes:
      - 'C:\Users\User\.config\earthengine:/root/.config/earthengine'

  ngrok:
    image: ngrok/ngrok:latest
    container_name: sar_ngrok_tunnel
    ports:
      - "4040:4040"
    depends_on:
      - app
    env_file:
      - .env
    # [Ultimate Final Fix] Use the --domain flag to assign a permanent address.
    command:
      - "http"
      - "app:8000"
      - "--log=stdout"
      - "--basic-auth=nasa:spaceapps2025"
      - "--response-header-add=Access-Control-Allow-Origin:*"
      - "--domain=juliette-unattempted-tammara.ngrok-free.dev" # <-- The magic happens here
